import React from 'react';

const Button = (props) => {
    return (
        
              <a href="#" type={props.tyle} class="hero_btn btn">{props.title}</a>
        
    );
}

export default Button;
